*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db

/lobbyhub.iml
*.sqlite
*.sqlite3

# Personal Claude Code permissions, not shared with the team
.claude/settings.local.json

# Game artwork is fetched from Steam by `php artisan games:artwork`
/public/images/games

# Snapshots and logs left behind by browser automation
.playwright-cli/

# Go binaries, built in place on the machine that runs them — the .bin suffix
# is what keeps `a2s-benchmark.bin` from clashing with the source directory of
# the same name.
*.bin
